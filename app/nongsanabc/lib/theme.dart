import 'dart:ui';

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class MyTheme {
  static const TextStyle normalFont = TextStyle(fontSize: 15);
  static const TextStyle heading1 = TextStyle(
      fontSize: 25, fontWeight: FontWeight.normal, fontFamily: 'Muli');
  static const Color colorButton = Color(0xFFE0E0E0);
  static const Brightness darkMode = Brightness.dark;
  static const Brightness lightMode = Brightness.light;
}
